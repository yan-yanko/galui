# ── Required ────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Firecrawl (highly recommended — handles JS-heavy sites) ─────────────────
FIRECRAWL_API_KEY=fc-...

# ── API Auth (leave empty to disable in dev) ────────────────────────────────
REGISTRY_API_KEY=

# ── Storage ─────────────────────────────────────────────────────────────────
DATABASE_URL=data/registry.db

# ── Service Identity ─────────────────────────────────────────────────────────
# Change to your production domain when deploying
BASE_API_URL=http://localhost:8000

# ── Crawl Settings ──────────────────────────────────────────────────────────
MAX_PAGES_PER_CRAWL=20

# ── LLM Models ──────────────────────────────────────────────────────────────
FAST_MODEL=claude-haiku-4-5-20251001
DEEP_MODEL=claude-sonnet-4-5-20250929

# ── Lemon Squeezy (billing) ──────────────────────────────────────────────────
# Get these from app.lemonsqueezy.com → Settings → Webhooks / Products
LS_WEBHOOK_SECRET=              # Webhook signing secret (Settings → Webhooks)
LS_VARIANT_STARTER=             # Variant ID for Starter $9/mo  (Product → Variants → ID)
LS_VARIANT_STARTER_ANNUAL=      # Variant ID for Starter $90/yr
LS_VARIANT_PRO=                 # Variant ID for Pro $29/mo
LS_VARIANT_PRO_ANNUAL=          # Variant ID for Pro $290/yr

# ── Email (Resend) ───────────────────────────────────────────────────────────
RESEND_API_KEY=             # re_... from resend.com
EMAIL_FROM=hello@galuli.io
